const INITIAL_STATE = {
  user: null,
}

const userReducer = (state = INITIAL_STATE, action) => {
//   console.log(action);
  switch (action.type) {
    default:
      return state;
  }
};

export default userReducer;